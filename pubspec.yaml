name: gospel_stream
description: A Gospel multimedia streaming platform for web, mobile, and desktop.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  video_player: ^2.6.0
  just_audio: ^0.10.0
  url_launcher: ^6.1.10
  google_fonts: ^4.0.3
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
